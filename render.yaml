services:
  - type: web
    name: whoami-server
    runtime: python
    buildCommand: pip install -r server/requirements.txt
    startCommand: python server/main.py
    envVars:
      - key: PORT
        value: 10000
